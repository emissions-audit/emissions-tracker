DATABASE_URL=postgresql+asyncpg://emissions:emissions@localhost:5432/emissions
ANTHROPIC_API_KEY=sk-ant-...
# REV-03: bearer token gating /v1/metrics/launch/admin (funnel/conversion stats).
# If unset, the admin endpoint fails closed (no caller can authenticate).
LAUNCH_METRICS_ADMIN_TOKEN=
